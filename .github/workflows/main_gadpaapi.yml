name: Build and deploy ASP.Net Core app to Azure Web App - gadpaapi

on:
  push:
    branches:
      - main
    paths:
      - 'GadpaApi/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Restore dependencies
        run: dotnet restore ./GadpaApi/GadpaApi.csproj

      - name: Build
        run: dotnet build ./GadpaApi/GadpaApi.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish ./GadpaApi/GadpaApi.csproj -c Release -o ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ./publish

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: .net-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_B31C7E5EEC7A4260B2E4C272911158CC }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_C5F789778E9E41F0A30F1288354AFF5F }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_F37CF0BF982E442FA2500AC1D097240A }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'gadpaapi'
          slot-name: 'Production'
          package: .